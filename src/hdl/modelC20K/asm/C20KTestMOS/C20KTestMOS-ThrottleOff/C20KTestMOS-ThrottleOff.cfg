MEMORY {
    ZP:     start = $00F0, size = $0010, type = rw, define = yes;
    STACK:  start = $0100, size = $0100, type = rw, define = yes;
    SVECS:  start = $0200, size = $0020, type = rw, define = yes;
    RAM:    start = $0220, size = $00E0, type = rw, define = yes;

    ROM:    start = $C000, size = $3C00, file = %O, fill=yes;
    HWHOLE: start = $FC00, size = $0300, file = %O, fill=yes;
    ROM2:   start = $FF00, size = $00FA, file = %O, fill=yes;
    VEC:    start = $FFFA, size = $6, file = %O, fill=yes;
}
SEGMENTS {
    CODE:     load = ROM, type = ro;
    RODATA:   load = ROM, type = ro;
    VECTORS:  load = VEC, type = ro;
    ZEROPAGE: load = ZP,  type = zp;
    DATA:     load = RAM, type = rw;
    BSS:      load = RAM, type = bss;
}
FEATURES {
}
SYMBOLS {
}


