# C20KBareMOS
=============

This is a boot ROM to be loaded at F000 on the C20KFirstLight firmware which
connects via the debug UART to a NoICE monitor.

You will need the NoIce software to communicate with this OS over the
debug UART.